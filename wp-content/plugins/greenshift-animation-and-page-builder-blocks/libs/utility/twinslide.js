document.addEventListener("DOMContentLoaded",function(){let n=document.querySelectorAll(".gs-twin-on-hover");n.forEach(n=>{let e=n.innerHTML,t=`<span>${e}</span>`;n.innerHTML=`${t}<span aria-hidden="true">${e}</span>`})});